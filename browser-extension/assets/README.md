# Assets

This folder contains static assets for the Agent0 Companion browser extension's new tab dashboard.

## Files

- **default-bg.svg** — Default background gradient used by the new tab page. Replace with a `.jpg` or `.png` if you prefer a photographic background. Update the CSS `body` background reference accordingly.
